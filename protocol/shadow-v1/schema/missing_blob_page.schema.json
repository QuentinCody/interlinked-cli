{
	"$id": "shadow-v1/missing_blob_page.schema.json",
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"additionalProperties": false,
	"description": "Shadow protocol v1 wire record \"missing_blob_page\", generated from types-transport.ts.",
	"properties": {
		"items": {
			"items": {
				"additionalProperties": false,
				"properties": {
					"blob_digest": {
						"pattern": "^[0-9a-f]{64}$",
						"type": "string",
						"x-brand": {
							"__digest": "blob"
						}
					},
					"max_bytes": {
						"minimum": 0,
						"type": "integer"
					},
					"put_url": {
						"type": "string"
					}
				},
				"required": [
					"blob_digest",
					"max_bytes",
					"put_url"
				],
				"type": "object"
			},
			"type": "array"
		},
		"missing_set": {
			"additionalProperties": false,
			"properties": {
				"missing_count": {
					"minimum": 0,
					"type": "integer"
				},
				"missing_set_digest": {
					"pattern": "^[0-9a-f]{64}$",
					"type": "string",
					"x-brand": {
						"__digest": "missing-set"
					}
				},
				"missing_set_id": {
					"pattern": "^[A-Za-z0-9][A-Za-z0-9._~-]{0,127}$",
					"type": "string",
					"x-brand": {
						"__brand": "opaque-id"
					}
				}
			},
			"required": [
				"missing_count",
				"missing_set_digest",
				"missing_set_id"
			],
			"type": "object"
		},
		"next_page_token": {
			"anyOf": [
				{
					"type": "null"
				},
				{
					"pattern": "^[A-Za-z0-9][A-Za-z0-9._~-]{0,127}$",
					"type": "string",
					"x-brand": {
						"__brand": "opaque-id"
					}
				}
			]
		}
	},
	"required": [
		"items",
		"missing_set",
		"next_page_token"
	],
	"title": "MissingBlobPageV1",
	"type": "object",
	"x-shadow-contract": "structural-shape",
	"x-shadow-contract-meaning": "Shape metadata derived from the TypeScript declarations only (keys, required sets, literals, brand patterns, integers). NOT the validator: byte/array bounds, calendar validity, path rules, set/sort semantics and cross-field rules live in the parsers. A second implementation must port field-checks.ts and the parse-*.ts modules and execute protocol/shadow-v1/fixtures/malformed-corpus.json; schema validity is necessary, not sufficient.",
	"x-shadow-record": {
		"id": "missing_blob_page",
		"kind": "wire",
		"module": "types-transport.ts",
		"type": "MissingBlobPageV1"
	}
}
