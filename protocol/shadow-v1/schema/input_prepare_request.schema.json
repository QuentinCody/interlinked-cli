{
	"$id": "shadow-v1/input_prepare_request.schema.json",
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"additionalProperties": false,
	"description": "Shadow protocol v1 wire record \"input_prepare_request\", generated from types-transport.ts.",
	"properties": {
		"client_scanner_policy_digest": {
			"pattern": "^[0-9a-f]{64}$",
			"type": "string",
			"x-brand": {
				"__digest": "scanner-policy"
			}
		},
		"idempotency_key": {
			"pattern": "^[A-Za-z0-9][A-Za-z0-9._~-]{0,127}$",
			"type": "string",
			"x-brand": {
				"__brand": "opaque-id"
			}
		},
		"schema_version": {
			"const": 1
		},
		"upload_id": {
			"pattern": "^[A-Za-z0-9][A-Za-z0-9._~-]{0,127}$",
			"type": "string",
			"x-brand": {
				"__brand": "opaque-id"
			}
		}
	},
	"required": [
		"client_scanner_policy_digest",
		"idempotency_key",
		"schema_version",
		"upload_id"
	],
	"title": "ShadowInputPrepareRequestV1",
	"type": "object",
	"x-shadow-contract": "structural-shape",
	"x-shadow-contract-meaning": "Shape metadata derived from the TypeScript declarations only (keys, required sets, literals, brand patterns, integers). NOT the validator: byte/array bounds, calendar validity, path rules, set/sort semantics and cross-field rules live in the parsers. A second implementation must port field-checks.ts and the parse-*.ts modules and execute protocol/shadow-v1/fixtures/malformed-corpus.json; schema validity is necessary, not sufficient.",
	"x-shadow-record": {
		"id": "input_prepare_request",
		"kind": "wire",
		"module": "types-transport.ts",
		"type": "ShadowInputPrepareRequestV1"
	}
}
